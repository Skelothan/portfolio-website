<%= form_with(model: file_project, local: true) do |form| %>
  <% if file_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file_project.errors.count, "error") %> prohibited this file_project from being saved:</h2>

      <ul>
      <% file_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :file_id %>
    <%= form.text_field :file_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
