<%= form_with(model: tag_project, local: true) do |form| %>
  <% if tag_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_project.errors.count, "error") %> prohibited this tag_project from being saved:</h2>

      <ul>
      <% tag_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.select :project_id, @projects %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.select :tag_id, @tags %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
